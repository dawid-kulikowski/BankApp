package application;

public class CurrencyAccount extends Account{
    public CurrencyAccount(String accountNumber, String accountType, int balance) {
        super(accountNumber, accountType, balance);
    }
}
